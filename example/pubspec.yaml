name: example
environment:
  sdk: ">=3.8.0 <4.0.0"

publish_to: none

dependencies:
  json_annotation: ^4.11.0
  coollocalizations:
    path: ../

dev_dependencies:
  coolint: ^2.1.2
  build_runner: any
  json_serializable: ^6.13.0
